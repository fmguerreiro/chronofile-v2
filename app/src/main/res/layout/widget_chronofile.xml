<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="horizontal"
    android:padding="8dp"
    android:gravity="center_vertical">

    <!-- App Icon -->
    <ImageView
        android:id="@+id/widget_app_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginEnd="8dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="Open Chronofile"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true" />

    <!-- Activity Buttons Container -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <Button
            android:id="@+id/widget_activity_1"
            style="@style/WidgetActivityButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginEnd="2dp"
            android:text="Work"
            tools:ignore="HardcodedText" />

        <Button
            android:id="@+id/widget_activity_2"
            style="@style/WidgetActivityButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginHorizontal="2dp"
            android:text="Break"
            tools:ignore="HardcodedText" />

        <Button
            android:id="@+id/widget_activity_3"
            style="@style/WidgetActivityButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginHorizontal="2dp"
            android:text="Lunch"
            tools:ignore="HardcodedText" />

        <Button
            android:id="@+id/widget_activity_4"
            style="@style/WidgetActivityButton"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="2dp"
            android:text="Meeting"
            tools:ignore="HardcodedText" />

    </LinearLayout>

</LinearLayout>